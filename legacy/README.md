# Legacy Archive

This folder contains archived V1/V2 scripts, docs, and tests that are no longer part of the active runtime path.

Active production entrypoint remains:

`python v3/pdf_watcher_v3.py`

Notes:
- Files here are retained for reference and rollback only.
- New development should happen only under `v3/` and `tests/v3/`.
