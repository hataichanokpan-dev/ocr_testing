# PDF Folder Watcher with OCR (ระบบตรวจสอบและเปลี่ยนชื่อไฟล์ PDF อัตโนมัติ)

โปรแกรมสำหรับตรวจสอบโฟลเดอร์ รอรับไฟล์ PDF จากเครื่อง Scanner แล้วอ่านข้อความจากส่วนหัวของเอกสารด้วย OCR เพื่อเปลี่ยนชื่อไฟล์อัตโนมัติ

## 🎯 ความสามารถ

- 📁 **ตรวจสอบโฟลเดอร์อัตโนมัติ** - เฝ้าดูโฟลเดอร์ที่กำหนดตลอดเวลา
- 📄 **อ่านข้อความจาก PDF** - รองรับทั้ง PDF ที่มี text layer และ PDF สแกน
- 🔤 **OCR ขั้นสูง** - ใช้ Tesseract OCR พร้อมเทคนิคประมวลผลภาพหลายแบบ
- 🧹 **ลบเส้นขีดเขียน** - กำจัดลายมือหรือเส้นที่ทับบนข้อความ
- ✏️ **เปลี่ยนชื่อไฟล์อัตโนมัติ** - ตั้งชื่อไฟล์ตามข้อความที่อ่านได้
- 🔧 **ปรับแต่งได้** - ตั้งค่าผ่านไฟล์ config.ini
- 📊 **Majority Voting** - เลือกผลลัพธ์ที่ถูกต้องที่สุดจากหลายวิธี
- 📝 **บันทึก Log** - ติดตามการทำงานและแก้ไขปัญหา

## 📋 ความต้องการระบบ

- Windows 10 หรือใหม่กว่า
- Python 3.7 ขึ้นไป
- Tesseract OCR

## 🚀 การติดตั้ง

### ขั้นตอนที่ 1: ติดตั้ง Python

1. ดาวน์โหลด Python จาก [python.org](https://www.python.org/downloads/)
2. ติดตั้ง Python (อย่าลืมเลือก "Add Python to PATH")
3. ตรวจสอบการติดตั้ง:
```powershell
python --version
```

### ขั้นตอนที่ 2: ติดตั้ง Tesseract OCR

1. ดาวน์โหลด Tesseract สำหรับ Windows:
   - เข้า [https://github.com/UB-Mannheim/tesseract/wiki](https://github.com/UB-Mannheim/tesseract/wiki)
   - ดาวน์โหลดไฟล์ติดตั้ง (เช่น `tesseract-ocr-w64-setup-5.3.3.20231005.exe`)

2. ติดตั้ง Tesseract:
   - เรียกใช้ไฟล์ติดตั้ง
   - เลือก path: `C:\Program Files\Tesseract-OCR` (แนะนำ)
   - ติดตั้งภาษาอังกฤษ (English) เพิ่มเติม

3. ตรวจสอบการติดตั้ง:
```powershell
& "C:\Program Files\Tesseract-OCR\tesseract.exe" --version
```

### ขั้นตอนที่ 3: ติดตั้ง Python Packages

เปิด PowerShell ในโฟลเดอร์โปรเจ็กต์แล้วรันคำสั่ง:

```powershell
pip install -r requirements.txt
```

Package ที่จะติดตั้ง:
- `watchdog` - ตรวจสอบการเปลี่ยนแปลงในโฟลเดอร์
- `PyMuPDF` - อ่านและประมวลผลไฟล์ PDF
- `Pillow` - ประมวลผลภาพ
- `pytesseract` - เชื่อมต่อกับ Tesseract OCR
- `opencv-python` - ประมวลผลภาพขั้นสูง
- `numpy` - คำนวณทางคณิตศาสตร์

## ⚙️ การตั้งค่า

### 1. แก้ไขไฟล์ config.ini

```ini
[Settings]
# โฟลเดอร์ที่ต้องการเฝ้าดู
watch_folder = C:\Scanned_PDFs

# โฟลเดอร์สำหรับบันทึกไฟล์ที่เปลี่ยนชื่อแล้ว (เว้นว่างเพื่อใช้โฟลเดอร์เดียวกัน)
output_folder = 

# หน้าที่ต้องการอ่าน (คั่นด้วยจุลภาค เช่น 1,2 หรือ 2)
pages_to_read = 2

# พื้นที่ส่วนหัวที่ต้องการอ่าน (เป็นเปอร์เซ็นต์ของหน้า)
header_area_top = 0.5      # เริ่มจากบนลงมา 0.5%
header_area_left = 15      # เริ่มจากซ้ายเข้ามา 15%
header_area_width = 38     # ความกว้าง 38%
header_area_height = 6     # ความสูง 6%

# ตัวเลือกการตั้งชื่อไฟล์
max_filename_length = 100
remove_special_chars = True
replace_spaces_with = _

# การตรวจสอบรูปแบบ (สำหรับให้คะแนนผลลัพธ์ OCR)
# รูปแบบที่คาดหวัง: B-F-GTX-S17893848
expected_parts = 4          # จำนวนส่วนที่คั่นด้วย -
expected_separator = -      # ตัวคั่น
expected_digit_count = 8    # จำนวนตัวเลขที่ต้องการ
min_digit_count = 6         # จำนวนตัวเลขขั้นต่ำ

# ตัวเลือกการประมวลผล
delete_original = False     # True = ย้ายไฟล์, False = คัดลอกไฟล์
add_timestamp = False       # เพิ่มวันที่-เวลาในชื่อไฟล์
file_extension = .pdf

[Logging]
log_level = INFO
log_file = pdf_watcher.log
```

### 2. สร้างโฟลเดอร์ที่ต้องการ

สร้างโฟลเดอร์สำหรับเก็บไฟล์ PDF จาก Scanner:

```powershell
New-Item -ItemType Directory -Path "C:\Scanned_PDFs" -Force
```

## 📖 วิธีใช้งาน

### เริ่มใช้งานโปรแกรม

1. เปิด PowerShell ในโฟลเดอร์โปรเจ็กต์:
```powershell
cd C:\Programing\Microchip\OCR_PDW
```

2. เรียกใช้โปรแกรม:
```powershell
python pdf_watcher.py
```

3. โปรแกรมจะแสดงข้อความ:
```
============================================================
PDF Folder Watcher Started
============================================================
Watching folder: C:\Scanned_PDFs
Monitoring started. Press Ctrl+C to stop.
```

4. วางไฟล์ PDF ในโฟลเดอร์ `C:\Scanned_PDFs`

5. โปรแกรมจะ:
   - ตรวจจับไฟล์ PDF ใหม่อัตโนมัติ
   - อ่านข้อความจากส่วนหัว
   - เปลี่ยนชื่อไฟล์ตามข้อความที่อ่านได้

### หยุดโปรแกรม

กด `Ctrl+C` ในหน้าต่าง PowerShell

## 🎨 ตัวอย่างการทำงาน

### ไฟล์ต้นฉบับ:
```
Test.pdf  (มีข้อความ "B-F-GTX-S17893848" ในส่วนหัว)
```

### ไฟล์หลังเปลี่ยนชื่อ:
```
B_F_GTX_S17893848.pdf
```

### Log การทำงาน:
```
2026-01-13 21:00:00 - INFO - New PDF detected: C:\Scanned_PDFs\Test.pdf
2026-01-13 21:00:00 - INFO - Processing PDF: C:\Scanned_PDFs\Test.pdf
2026-01-13 21:00:00 - INFO - PDF has 2 pages. Will read pages: [2]
2026-01-13 21:00:00 - INFO - Processing page 2...
2026-01-13 21:00:00 - INFO - Method 7 (gradient): B-F-GTX-S17893848
2026-01-13 21:00:00 - INFO - Best result selected: B-F-GTX-S17893848 (score: 230, frequency: 5/6)
2026-01-13 21:00:00 - INFO - Copied and renamed: Test.pdf -> B_F_GTX_S17893848.pdf
```

## 🔧 การปรับแต่งขั้นสูง

### ปรับพื้นที่การอ่าน

หากอ่านข้อความไม่ถูกต้อง ให้ดูไฟล์ debug ที่สร้างขึ้น:
- `debug_page1_extracted_area.png` - พื้นที่ที่ตัดจากหน้า 1
- `debug_page2_extracted_area.png` - พื้นที่ที่ตัดจากหน้า 2

แล้วปรับค่าใน config.ini:
```ini
header_area_top = 0.5      # เลื่อนขึ้น/ลง
header_area_left = 15      # เลื่อนซ้าย/ขวา
header_area_width = 38     # เพิ่ม/ลดความกว้าง
header_area_height = 6     # เพิ่ม/ลดความสูง
```

### ดู Debug Images

โปรแกรมจะสร้างภาพ debug จากทุกวิธีการประมวลผล:
- `debug_method0_hough.png` - ลบเส้นด้วย Hough Transform
- `debug_method0B_multikernel.png` - ลบเส้นหลายทิศทาง
- `debug_method0C_components.png` - กรองเฉพาะตัวอักษร
- `debug_method1_lineremoval.png` - ลบเส้นแนวนอน
- `debug_method2_threshold.png` - Threshold สูง
- `debug_method3_adaptive.png` - Adaptive threshold
- `debug_method4_otsu.png` - OTSU algorithm
- `debug_method5_bilateral.png` - Bilateral filter
- `debug_method6_blackhat.png` - Black hat transform
- `debug_method7_gradient.png` - Morphological gradient

### ประมวลผลไฟล์ที่มีอยู่แล้ว

สร้างไฟล์ `process_existing.py`:

```python
import os
from configparser import ConfigParser
from pdf_extractor import PDFTextExtractor
from pdf_watcher import PDFFileHandler

# โหลด config
config = ConfigParser()
config.read('config.ini')

# สร้าง objects
extractor = PDFTextExtractor(config)
handler = PDFFileHandler(config, extractor)

# ประมวลผลไฟล์ทั้งหมดในโฟลเดอร์
watch_folder = config.get('Settings', 'watch_folder')
for file in os.listdir(watch_folder):
    if file.lower().endswith('.pdf'):
        file_path = os.path.join(watch_folder, file)
        print(f"Processing: {file}")
        handler.process_pdf(file_path)
        print("---")
```

รันด้วย:
```powershell
python process_existing.py
```

## 📊 วิธีการประมวลผล OCR

โปรแกรมใช้เทคนิคหลายแบบเพื่อความแม่นยำสูงสุด:

1. **Hough Line Removal** - ตรวจจับและลบเส้นตรง
2. **Multi-Kernel Line Removal** - ลบเส้นทุกทิศทาง (นอน ตั้ง เฉียง)
3. **Connected Components** - กรองเฉพาะ component ที่เหมือนตัวอักษร
4. **Morphological Gradient** - เน้นขอบของตัวอักษร
5. **Adaptive Threshold** - ปรับ threshold ตามแต่ละพื้นที่
6. **OTSU Algorithm** - หา threshold อัตโนมัติ
7. **Bilateral Filter** - ลด noise แต่รักษาขอบ

### Majority Voting

โปรแกรมเลือกผลลัพธ์ตามลำดับ:
1. **คะแนน (Score)** - ผลที่ตรงกับรูปแบบที่กำหนด
2. **ความถี่ (Frequency)** - ผลที่ปรากฏบ่อยที่สุด
3. **ความยาว (Length)** - ผลที่สั้นกว่า (error น้อยกว่า)

## 🐛 การแก้ปัญหา

### ปัญหา: Tesseract not found

```
Error: tesseract is not installed or it's not in your PATH
```

**วิธีแก้:**
1. ตรวจสอบว่าติดตั้ง Tesseract แล้ว
2. ตรวจสอบ path ใน `pdf_extractor.py` บรรทัด 17:
```python
tesseract_path = r'C:\Program Files\Tesseract-OCR\tesseract.exe'
```

### ปัญหา: อ่านข้อความไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบไฟล์ `debug_page*_extracted_area.png` ว่าตำแหน่งถูกต้องหรือไม่
2. ปรับค่าพื้นที่ในไฟล์ `config.ini`
3. ลองเปลี่ยนหน้าที่อ่าน: `pages_to_read = 1` หรือ `pages_to_read = 1,2`
4. ดูไฟล์ debug methods ต่างๆ เพื่อหาวิธีที่ดีที่สุด

### ปัญหา: โฟลเดอร์ไม่มีอยู่

```
ERROR - Watch folder does not exist: C:\Scanned_PDFs
```

**วิธีแก้:**
```powershell
New-Item -ItemType Directory -Path "C:\Scanned_PDFs" -Force
```

### ปัญหา: ไฟล์ถูกประมวลผลซ้ำ

**วิธีแก้:**
1. ตั้งค่า `delete_original = True` ใน config.ini เพื่อย้ายไฟล์แทนการคัดลอก
2. หรือตั้งค่า `output_folder` ให้แยกจากโฟลเดอร์ต้นฉบับ

## 📁 โครงสร้างโปรเจ็กต์

```
OCR_PDW/
├── pdf_watcher.py          # โปรแกรมหลัก
├── pdf_extractor.py        # โมดูลสำหรับอ่านข้อความจาก PDF
├── config.ini              # ไฟล์ตั้งค่า
├── requirements.txt        # Python packages ที่ต้องใช้
├── README.md              # คู่มือภาษาอังกฤษ
├── README.th.md           # คู่มือภาษาไทย (ไฟล์นี้)
├── pdf_watcher.log        # Log การทำงาน (สร้างเมื่อรันโปรแกรม)
└── debug_*.png            # ภาพ debug (สร้างเมื่อประมวลผล PDF)
```

## 🚀 เรียกใช้อัตโนมัติเมื่อเปิดเครื่อง

### วิธีที่ 1: Windows Service (แนะนำ) ⭐

วิธีนี้ดีที่สุดสำหรับการทำงานอัตโนมัติและเสถียร:

**ขั้นตอนติดตั้ง:**

1. **ดาวน์โหลด NSSM:**
   - เข้า [https://nssm.cc/download](https://nssm.cc/download)
   - ดาวน์โหลด nssm-2.24.zip (หรือเวอร์ชั่นล่าสุด)
   - แตกไฟล์และคัดลอก `nssm.exe` จากโฟลเดอร์ `win64` มาไว้ที่:
     ```
     C:\Programing\Microchip\OCR_PDW\
     ```

2. **ติดตั้ง Service:**
   - คลิกขวา `install_service.bat`
   - เลือก **"Run as administrator"**
   - ทำตามขั้นตอนที่ปรากฏ

3. **ตรวจสอบ Service:**
   ```powershell
   python service_manager.py status
   ```

**จัดการ Service:**
```powershell
# เริ่ม service
net start PDFWatcher

# หยุด service
net stop PDFWatcher

# ดูสถานะ
python service_manager.py status

# ถอนการติดตั้ง
python service_manager.py uninstall
```

**ข้อดีของ Windows Service:**
- ✅ เริ่มทำงานอัตโนมัติเมื่อ Windows เริ่มต้น
- ✅ ทำงานแบบ background ไม่ต้อง login
- ✅ Restart อัตโนมัติเมื่อเกิดข้อผิดพลาด
- ✅ จัดการง่ายผ่าน Windows Services

📖 **คู่มือโดยละเอียด:** ดูที่ [SERVICE_SETUP.md](SERVICE_SETUP.md)

### วิธีที่ 2: Task Scheduler

1. เปิด Task Scheduler
2. สร้าง Basic Task ใหม่
3. ตั้งค่า Trigger: "When the computer starts"
4. ตั้งค่า Action:
   - Program: `python.exe`
   - Arguments: `pdf_watcher.py`
   - Start in: `C:\Programing\Microchip\OCR_PDW`

### วิธีที่ 3: สร้างไฟล์ .bat

สร้างไฟล์ `start_watcher.bat`:

```batch
@echo off
cd /d C:\Programing\Microchip\OCR_PDW
python pdf_watcher.py
pause
```

วางไฟล์ใน Startup folder:
```
C:\Users\{YourUsername}\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup
```

## 📝 License

โปรเจ็กต์นี้สร้างขึ้นเพื่อใช้งานภายในองค์กร

## 🤝 การสนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบไฟล์ `pdf_watcher.log`
2. ดูภาพ debug ที่สร้างขึ้น
3. ตรวจสอบการตั้งค่าใน `config.ini`

---

**หมายเหตุ:** โปรแกรมนี้ออกแบบมาสำหรับไฟล์ PDF จาก Scanner โดยเฉพาะ และทำงานได้ดีที่สุดกับ PDF ที่มีข้อความเป็นภาษาอังกฤษและตัวเลข
